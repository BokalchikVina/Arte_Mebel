import { describe, it } from 'vitest'

describe('TailorGlass landing integration', () => {
  it.skip('hydrates hero metrics from site model and keeps continuity state across sections', () => {
    // Skeleton for integration test:
    // 1. Render <App /> with Testing Library.
    // 2. Assert that hero metrics appear with values (14 дн, 347, 99%).
    // 3. Scroll or interact with Showcase scene and ensure continuity hint remains visible.
  })

  it.todo(
    'propagates CTA clicks to Yandex goal + updates UI copy (critical marketing path)',
  )
})
