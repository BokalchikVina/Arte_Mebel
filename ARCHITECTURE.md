# 🏗️ Архитектура проекта

## Технологический стек

### Core
- **Next.js 14** - React framework с App Router
- **TypeScript 5.3** - типизация
- **React 18** - UI библиотека

### Styling
- **Tailwind CSS 3.4** - utility-first CSS
- **CSS Modules** - модульные стили (если нужно)
- **Framer Motion 10** - анимации

### Архитектурные паттерны
- **Atomic Design** - организация компонентов
- **Component-Driven Development** - разработка снизу вверх
- **Server-First** - приоритет серверных компонентов

## Структура директорий

```
workspace/
│
├── app/                          # Next.js App Router
│   ├── layout.tsx               # Root layout
│   ├── page.tsx                 # Home page
│   ├── globals.css              # Global styles import
│   └── api/                     # API routes (future)
│       └── contact/
│           └── route.ts
│
├── components/                   # UI Components (Atomic Design)
│   │
│   ├── atoms/                   # Неделимые базовые компоненты
│   │   ├── Button.tsx           # iOS-style кнопка
│   │   ├── Card.tsx             # Glass карточка
│   │   ├── Input.tsx            # Поле ввода с валидацией
│   │   └── Badge.tsx            # Бейдж/Тег
│   │
│   ├── molecules/               # Комбинации атомов
│   │   ├── ProductCard.tsx      # Карточка товара
│   │   ├── Navigation.tsx       # Навигационное меню
│   │   ├── Hero.tsx             # Hero секция
│   │   └── ContactForm.tsx      # Форма контактов
│   │
│   └── organisms/               # Сложные компоненты
│       ├── CatalogSection.tsx   # Секция каталога
│       ├── AboutSection.tsx     # О компании
│       ├── ContactsSection.tsx  # Контакты
│       └── Footer.tsx           # Подвал сайта
│
├── lib/                         # Утилиты и хелперы
│   ├── constants.ts             # Константы (конфиг сайта, навигация)
│   ├── utils.ts                 # Вспомогательные функции
│   └── mockData.ts              # Тестовые данные (временно)
│
├── types/                       # TypeScript определения
│   └── index.ts                 # Все типы проекта
│
├── styles/                      # Стили
│   └── globals.css              # Tailwind + кастомные стили
│
├── public/                      # Статические файлы
│   ├── robots.txt               # SEO
│   ├── site.webmanifest         # PWA manifest
│   └── [images]                 # Изображения
│
└── [config files]               # Конфигурационные файлы
    ├── package.json
    ├── tsconfig.json
    ├── tailwind.config.ts
    ├── next.config.js
    └── .eslintrc.json
```

## Atomic Design детально

### Level 1: Atoms (Атомы)
**Цель**: Неделимые UI элементы

```
atoms/
├── Button.tsx      # Базовая кнопка с вариантами
├── Card.tsx        # Glass карточка-контейнер
├── Input.tsx       # Текстовое поле
└── Badge.tsx       # Лейбл/тег
```

**Характеристики**:
- Не содержат бизнес-логику
- Максимально переиспользуемые
- Полностью типизированные
- Имеют пропсы для кастомизации

**Пример использования**:
```typescript
<Button variant="primary" size="lg">
  Нажми меня
</Button>
```

### Level 2: Molecules (Молекулы)
**Цель**: Функциональные группы атомов

```
molecules/
├── ProductCard.tsx     # Card + Image + Button + Badge
├── Navigation.tsx      # Меню навигации
├── Hero.tsx           # Героическая секция
└── ContactForm.tsx    # Input + Input + Button
```

**Характеристики**:
- Комбинируют атомы
- Имеют специфичное назначение
- Могут содержать простую логику
- Обрабатывают события

**Пример использования**:
```typescript
<ProductCard
  product={product}
  onOrderClick={handleOrder}
/>
```

### Level 3: Organisms (Организмы)
**Цель**: Сложные секции страницы

```
organisms/
├── CatalogSection.tsx      # Полная секция каталога
├── AboutSection.tsx        # Секция "О нас"
├── ContactsSection.tsx     # Секция контактов
└── Footer.tsx              # Футер сайта
```

**Характеристики**:
- Комбинируют молекулы и атомы
- Содержат бизнес-логику
- Управляют состоянием
- Самодостаточные блоки

**Пример использования**:
```typescript
<CatalogSection />
```

### Level 4: Pages (Страницы)
**Цель**: Композиция организмов

```
app/
├── page.tsx           # Home page
├── layout.tsx         # Root layout
└── [future pages]/
```

**Характеристики**:
- Собирают организмы
- Определяют маршруты
- Передают данные компонентам
- Управляют мета-тегами

## Data Flow (Поток данных)

```
┌─────────────────────────────────────────┐
│         Next.js App Router              │
│  (Server Components by default)         │
└───────────────┬─────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────┐
│          Page Component                  │
│  • Получает данные (fetch/API)          │
│  • Передает в organisms                  │
└───────────────┬─────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────┐
│         Organism Component               │
│  • Управляет состоянием секции          │
│  • Координирует molecules                │
└───────────────┬─────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────┐
│        Molecule Component                │
│  • Обрабатывает события                 │
│  • Композирует atoms                     │
└───────────────┬─────────────────────────┘
                │
                ↓
┌─────────────────────────────────────────┐
│          Atom Component                  │
│  • Рендерит UI                          │
│  • Применяет стили                       │
└─────────────────────────────────────────┘
```

## State Management

### Local State (useState)
Используется в компонентах для:
- UI состояние (открыто/закрыто)
- Формы
- Простые взаимодействия

```typescript
const [isOpen, setIsOpen] = useState(false);
const [formData, setFormData] = useState({});
```

### URL State (useSearchParams)
Для:
- Фильтры каталога
- Пагинация
- Сохраняемые настройки

```typescript
const searchParams = useSearchParams();
const category = searchParams.get('category');
```

### Server State (будущее)
Для данных с сервера:
- Товары
- Заказы
- Пользователи

**Рекомендуется**: React Query / SWR

## Styling Strategy

### Tailwind Utility Classes
Для базовой стилизации:
```typescript
<div className="flex items-center gap-4 p-6 rounded-2xl">
```

### Custom CSS Classes
Для сложных эффектов (globals.css):
```css
.glass {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(20px);
}
```

### Inline Styles
Только для динамических значений:
```typescript
<div style={{ width: `${progress}%` }}>
```

### Framer Motion
Для анимаций:
```typescript
<motion.div
  initial={{ opacity: 0 }}
  animate={{ opacity: 1 }}
/>
```

## Performance Architecture

### Server Components (по умолчанию)
```typescript
// app/page.tsx - Server Component
export default async function Page() {
  const data = await fetchData(); // На сервере
  return <Component data={data} />;
}
```

### Client Components
Только когда нужно:
```typescript
'use client';

// Нужны hooks, события, браузер API
export const InteractiveComponent = () => {
  const [state, setState] = useState();
  return <div onClick={...} />;
};
```

### Image Optimization
```typescript
import Image from 'next/image';

<Image
  src="/product.jpg"
  alt="Product"
  width={800}
  height={800}
  loading="lazy"  // Автоматически
  quality={85}
/>
```

### Code Splitting
Автоматически через:
- App Router (маршруты)
- Dynamic imports (компоненты)

```typescript
const HeavyComponent = dynamic(() => import('./Heavy'));
```

## SEO Architecture

### Metadata API
```typescript
// app/layout.tsx
export const metadata: Metadata = {
  title: 'Site Title',
  description: 'Site Description',
  openGraph: { ... },
};
```

### Structured Data (будущее)
```typescript
<script type="application/ld+json">
  {JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Product",
    ...
  })}
</script>
```

## Security Architecture

### Environment Variables
```
NEXT_PUBLIC_* - доступны клиенту
Без префикса - только на сервере
```

### API Routes (будущее)
```typescript
// app/api/contact/route.ts
export async function POST(request: Request) {
  // Валидация
  // Rate limiting
  // Обработка
  return Response.json({ success: true });
}
```

## Deployment Architecture

```
┌──────────────┐
│   Developer  │
│   (local)    │
└──────┬───────┘
       │ git push
       ↓
┌──────────────┐
│   GitHub     │
└──────┬───────┘
       │ webhook
       ↓
┌──────────────┐
│   Vercel     │
│   • Build    │
│   • Deploy   │
│   • CDN      │
└──────┬───────┘
       │
       ↓
┌──────────────┐
│    Users     │
│  (visitors)  │
└──────────────┘
```

## Monitoring & Analytics

### Yandex Metrika
```javascript
ym(ID, 'init', {
  clickmap: true,
  trackLinks: true,
  accurateTrackBounce: true,
  webvisor: true,
});
```

### Performance Monitoring
- Vercel Analytics (встроено)
- Lighthouse CI (CI/CD)
- Web Vitals tracking

## Future Enhancements

### Планируемые улучшения:

1. **Backend Integration**
   - REST API или GraphQL
   - Database (PostgreSQL/MongoDB)
   - Authentication (NextAuth.js)

2. **State Management**
   - Zustand/Redux для глобального состояния
   - React Query для server state

3. **Testing**
   - Jest + React Testing Library
   - Playwright для E2E
   - Storybook для компонентов

4. **CMS Integration**
   - Headless CMS (Strapi/Contentful)
   - Admin панель

5. **Advanced Features**
   - 3D Product Viewer
   - AR примерка мебели
   - AI recommendations

---

**Архитектура спроектирована для масштабируемости и простоты поддержки** 🚀
